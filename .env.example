# YouTube Data API v3 のキー。
#
# 本番: Cloudflare Pages ダッシュボードの「シークレット」に YOUTUBE_API_KEY として登録。
# ローカル: このファイルを `.dev.vars` にコピーして値を設定(`.dev.vars` は gitignore 済み)。
#
# リポジトリにもビルド成果物にも実キーを含めないこと。
YOUTUBE_API_KEY=your-youtube-data-api-v3-key-here
